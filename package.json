{
  "name": "children-service-checkin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "npx http-server . -p 8000",
    "serve": "npx http-server . -p 8000",
    "deploy": "npx firebase-tools deploy --only hosting"
  },
  "devDependencies": {
    "firebase-tools": "^12.0.0",
    "http-server": "^14.1.1"
  }
}
